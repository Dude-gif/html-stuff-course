title: html-stuff
description: starting to do some html
template:
  repo: html-stuff-template
  name: html-stuff
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
